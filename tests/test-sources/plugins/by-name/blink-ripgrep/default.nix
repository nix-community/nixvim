{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-ripgrep.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-ripgrep = {
      enable = true;
    };
  };
}
