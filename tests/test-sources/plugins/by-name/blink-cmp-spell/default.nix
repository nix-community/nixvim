{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-spell.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-spell = {
      enable = true;
    };
  };
}
