{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-git.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-git = {
      enable = true;
    };
  };
}
