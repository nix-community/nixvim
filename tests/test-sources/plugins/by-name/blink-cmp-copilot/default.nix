{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-copilot.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-copilot = {
      enable = true;
    };
  };
}
