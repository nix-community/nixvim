{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-emoji.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-emoji = {
      enable = true;
    };
  };
}
