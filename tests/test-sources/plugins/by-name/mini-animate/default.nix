{
  empty = {
    plugins.mini-animate.enable = true;
  };

  defaults = {
    plugins.mini-animate = {
      enable = true;
      settings = {
        cursor.enable = true;
        scroll.enable = true;
        resize.enable = true;
        open.enable = true;
        close.enable = true;
      };
    };
  };
}
