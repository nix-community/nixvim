{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-copilot.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-copilot = {
      enable = true;
    };
  };
}
