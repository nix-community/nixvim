{
  empty = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-dictionary.enable = true;
  };

  defaults = {
    plugins.blink-cmp.enable = true;
    plugins.blink-cmp-dictionary = {
      enable = true;
    };
  };
}
