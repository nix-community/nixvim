{
  empty = {
    plugins.copilot-cmp = {
      enable = true;
      cmp.enable = false;
    };
  };

  defaults = {
    plugins = {
      cmp.enable = true;
      copilot-cmp.enable = true;
    };
  };
}
